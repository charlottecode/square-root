
number = float(input("Enter a number to calculate its square root: "))

approximation = number / 2  

approximation = (approximation + number / approximation) / 2
approximation = (approximation + number / approximation) / 2
approximation = (approximation + number / approximation) / 2
approximation = (approximation + number / approximation) / 2
approximation = (approximation + number / approximation) / 2

print("The square root of (number) is approximately (approximation:.4")
